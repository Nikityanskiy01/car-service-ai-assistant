# Bind порты только на loopback; наружу публикует Nginx (80/443).
FRONTEND_BIND_ADDR=127.0.0.1
FRONTEND_PORT=8080
BACKEND_BIND_ADDR=127.0.0.1
BACKEND_PORT=3000

POSTGRES_USER=fox
POSTGRES_PASSWORD=change-this-strong-password
POSTGRES_DB=foxmotors

# Дефолтная cloud-модель для backend.
LLM_MODEL=qwen/qwen3-coder-next
