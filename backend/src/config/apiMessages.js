/**
 * Пользовательские тексты ошибок API (русский), коды в поле `code` без изменений.
 */

export const apiMessages = {
  serviceRequest: {
    /** Консультация не доведена до результата по обязательным полям */
    incompleteConsultation:
      'Сначала завершите консультацию: укажите пробег, симптомы и условия, при которых проявляется неисправность.',
    guestNamePhoneRequired: 'Укажите имя и телефон (поля fullName и phone).',
    /** Оптимистичная блокировка при смене статуса заявки */
    expectedVersionRequired:
      'Укажите expectedVersion — текущую версию заявки (целое число), чтобы избежать конфликта при одновременном изменении.',
  },
};
